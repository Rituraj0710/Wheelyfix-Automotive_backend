# MongoDB Connection String
# Option 1: MongoDB Atlas (cloud)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/wheelyfix?retryWrites=true&w=majority

# Option 2: Local MongoDB (recommended for development)
# MONGODB_URI=mongodb://localhost:27017/wheelyfix

# JWT Secret for Authentication
JWT_SECRET=your_jwt_secret_key_here

# Node Environment
NODE_ENV=development

# Server Port
PORT=5000

# Frontend URL for CORS (if needed)
FRONTEND_URL=http://localhost:8080